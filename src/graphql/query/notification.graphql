query getUserNotifications($unreadOnly: Boolean!) {
  getUserNotifications(unreadOnly: $unreadOnly) {
    success
    message
    unreadCount
    notifications {
      notificationId
      senderId
      receiverId
      message
      isRead
      createdAt
    }
  }
}
